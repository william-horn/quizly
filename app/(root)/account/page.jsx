import { Box, Typography, Container } from "@mui/material";

const Account = () => {
  return (
    <Box height="100vh">
      <Container>
        <Typography>Account (nothing here yet)</Typography>
      </Container>
    </Box>
  );
};

export default Account;
